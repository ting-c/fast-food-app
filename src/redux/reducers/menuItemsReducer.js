import menuItems from './../items-store/menuItems';

const INITIAL_STATE = {
  menuItems
};

const menuItemsReducers = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default menuItemsReducers;